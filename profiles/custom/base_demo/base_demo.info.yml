name: Base Demo
type: profile
description: 'A Drupal distribution for base demo.'
core_version_requirement: ^9 || ^10
base profile: varbase

dependencies:
  - node
  - block
  - block_content
  - dblog
  - page_cache
  - dynamic_page_cache

# You can specify themes, for example:
# themes:
#   - your_theme_name
